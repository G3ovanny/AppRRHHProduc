
import { parseISO } from 'date-fns'

export const convertfechasTrab = (trabajadores = []) => {

    console.log(trabajadores.fecha_nacimiento)
    //trabajadores.fecha_nacimiento = parseISO(trabajadores.fecha_nacimiento);

    return trabajadores
}
